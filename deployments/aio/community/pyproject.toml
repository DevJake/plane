[project]
name = "plane-aio-runtime"
version = "0.1.0"
# The runner inherits the selected backend image; released images currently span Python 3.12–3.14.
requires-python = ">=3.12,<3.15"
dependencies = [
    "supervisor==4.3.0",
]

[tool.uv]
package = false
required-version = "==0.12.1"
